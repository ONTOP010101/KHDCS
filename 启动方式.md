# 项目启动方式

## 前端 (Vue 3 + Vite)

```powershell
cd D:\客户端测试\frontend
npm run dev
```

- 端口: `3000`

## 后端 (Spring Boot 3.2.5)

```powershell
cd D:\客户端测试\backend
C:\Users\wd439\apache-maven-3.9.6\bin\mvn spring-boot:run
```

或者直接 IDEA 运行 `com.app.Application`

- 端口: `8080`
- 数据库: MySQL `photo_management` (root/123456)

## 快速验证

```powershell
# 前端
curl.exe -s -o NUL -w "%{http_code}" "http://localhost:3000"
# 预期: 200

# 后端
curl.exe -s "http://localhost:8080/samples/list?page=1&size=1"
# 预期: {"code":401,"message":"未登录或token已过期"} (正常, 表示服务在运行)
```

## 注意事项

- Maven 路径: `C:\Users\wd439\apache-maven-3.9.6`
- JDK 路径: `C:\Users\wd439\.jdks\jbr-17.0.14`
- MySQL 连接: `mysql -u root -p123456`
- DevTools 热重启已关闭 (`application.yml` 中 `spring.devtools.restart.enabled: false`)，避免 ApplicationContext 被关闭导致 500 错误
