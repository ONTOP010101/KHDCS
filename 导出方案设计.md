# 拍照管理系统 - 批量导出方案设计

## 一、方案概述

**目标**：支持 5000 条样品数据（含图片）的完整导出  
**交付物**：一个 ZIP 压缩包，内含原始图片 + 嵌入缩放图片的 Excel 产品目录

### 导出包结构

```
样品数据导出_20260521_143000.zip
├── 产品目录.xlsx              ← Excel，嵌入缩放图片（800×600，~80KB/张）
├── images/                    ← 原始图片文件夹
│   ├── ABC001/               ← 按公司编号分组
│   │   ├── ABC001_01.jpg     ← 原图（1MB）
│   │   ├── ABC001_02.jpg     ← 原图（1MB）
│   │   └── ...
│   ├── ABC002/
│   └── ...
```

---

## 二、方案对比

| 方案 | 文件大小 | 生成方式 | 用户体验 | 结论 |
|------|---------|----------|----------|------|
| Excel 嵌入原图 | ~5GB | 全量加载 → OOM | 打不开 | ❌ 不可行 |
| Excel 嵌入缩放图（本方案） | ~400MB | 流式生成 → 30MB内存 | 清晰可用 | ✅ **采用** |
| ZIP 原图 + Excel 缩放图 | ~5.4GB | 流式打包 → 30MB内存 | 可接受 | ✅ **采用** |

---

## 三、Excel 设计

### 3.1 工作表结构

| 列 | 内容 | 说明 |
|----|------|------|
| A | 序号 | 1-5000 |
| B | 公司编号 | sampleCode |
| C | 出厂货号 | factoryCode |
| D | 样品名称 | sampleName |
| E | 种类名称 | category |
| F | 厂商名称 | supplier |
| G | 出厂价 | factoryPrice |
| H | 图片预览 | 嵌入缩放图片（800×600，JPEG 质量 0.85） |
| I | 图片数量 | 该样品关联的图片数 |
| J | 备注 | remark |

### 3.2 图片缩放参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 最大宽度 | 800px | 产品目录足够清晰 |
| 最大高度 | 600px | 保持 4:3 比例 |
| 格式 | JPEG | 体积最小 |
| 质量 | 0.85 | 视觉无损 |
| 预估单张 | ~80KB | 5000张 ≈ 400MB |

---

## 四、技术架构

### 4.1 核心原则

> **永远不要一次性把 5000 张图片全部加载到内存里。**
> 所有操作都使用流式处理，内存稳定在 50MB 以内。

### 4.2 生成流程

```
┌──────────────────────────────────────────────────────┐
│                   导出流程（流式）                      │
├──────────────────────────────────────────────────────┤
│                                                      │
│  1. 查询样品数据（分页，每批 500 条）                    │
│       │                                              │
│       ▼                                              │
│  2. 创建 ZipOutputStream（直接写 HTTP Response）        │
│       │                                              │
│       ▼                                              │
│  3. 创建 SXSSFWorkbook（流式 Excel，内存只存 100 行）    │
│       │                                              │
│       ├─→ 逐条处理样品：                                │
│       │   ├─ 写数据行                                  │
│       │   ├─ 读取原图 → 缩放 → 嵌入 Excel              │
│       │   ├─ 释放已处理图片内存                         │
│       │   └─ 将原图流式复制到 ZIP                      │
│       │                                              │
│       ▼                                              │
│  4. Excel 写入 ZIP 条目                               │
│       │                                              │
│       ▼                                              │
│  5. 完成，响应头设置 Content-Disposition               │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 4.3 关键代码概要

```java
// 控制器
@GetMapping("/export")
public void export(@RequestParam(required = false) String keyword,
                   HttpServletResponse response) {
    response.setContentType("application/zip");
    response.setHeader("Content-Disposition",
        "attachment; filename=\"export_" + date + ".zip\"");
    
    try (ZipOutputStream zos = new ZipOutputStream(response.getOutputStream())) {
        // Step 1: 创建流式 Excel
        SXSSFWorkbook workbook = new SXSSFWorkbook(100); // 内存只保留100行
        
        // Step 2: 分页查询样品
        for (int page = 1; ; page++) {
            List<Sample> samples = sampleService.page(page, 500);
            if (samples.isEmpty()) break;
            
            for (Sample s : samples) {
                // 写数据行
                Row row = sheet.createRow(rowNum++);
                // ...
                
                // 查询该样品的图片
                List<Image> images = imageService.listBySampleId(s.getId());
                
                for (Image img : images) {
                    // 读取原图 → 缩放 → 嵌入 Excel
                    byte[] scaled = scaleImage(img.getFilePath(), 800, 600);
                    int idx = workbook.addPicture(scaled, WORKBOOK.PICTURE_TYPE_JPEG);
                    // 嵌入到图片列
                    
                    // 原图流式写入 ZIP
                    zos.putNextEntry(new ZipEntry("images/" + s.getSampleCode() + "/" + img.getFileName()));
                    Files.copy(Paths.get(img.getFilePath()), zos);
                    zos.closeEntry();
                }
            }
        }
        
        // Step 3: Excel 写入 ZIP
        zos.putNextEntry(new ZipEntry("产品目录.xlsx"));
        workbook.write(zos);
        zos.closeEntry();
        
        workbook.dispose(); // 清理临时文件
    }
}
```

---

## 五、长时间使用性能影响分析

### 5.1 影响矩阵

| 维度 | 影响程度 | 分析 | 对策 |
|------|---------|------|------|
| **磁盘空间** | ⚠️ 中 | SXSSFWorkbook 临时文件 | 定期清理 /tmp/poi-* |
| **CPU** | ⚠️ 中 | 5000次图片缩放 | 限制并发导出数 |
| **内存** | ✅ 低 | 流式处理，稳定 ~50MB | 无需特殊处理 |
| **数据库** | ✅ 低 | 分页查询，无大事务 | 索引已满足 |
| **磁盘 I/O** | ⚠️ 中 | 读5000张图 + 写ZIP | SSD 可接受 |
| **并发导出** | 🔴 关键 | 2人同时导出 = 10000次缩放 | 必须限制 + 排队 |

### 5.2 各项详细分析

#### 磁盘空间
```
问题：SXSSFWorkbook 会将临时行数据写入磁盘临时文件
     位置：System.getProperty("java.io.tmpdir")
     大小：5000行 × ~1KB/行 = ~5MB（可忽略）
     图片原图不用复制，直接流式传输到 ZIP
     
结论：✅ 不影响，临时文件很小
```

#### CPU（图片缩放）
```
单张缩放：800×600 JPEG 编码 ≈ 30ms
5000张：30ms × 5000 = 150秒 ≈ 2.5分钟

如果 2 人同时导出：300秒 ≈ 5分钟
CPU 满载：会阻塞其他正常请求

对策：
1. 限制同时只能 1 个导出任务
2. 导出使用独立线程池（1-2个线程）
3. 导出期间前端显示进度
```

#### 内存
```
流式处理模型：
- SXSSFWorkbook：只保留最后100行在内存中
- ZipOutputStream：缓冲 8KB
- 图片处理：每次只加载1张图，处理完立即释放
- GC 友好：大量短生命周期对象，Young GC 即可回收

峰值内存 ≈ 50MB（一张800×600图片解码后 ~1.5MB + Excel缓冲 + ZIP缓冲）

结论：✅ 内存安全，不会 OOM
```

#### 数据库
```
查询方式：分页查询，每批 500 条
SELECT * FROM samples LIMIT 500 OFFSET 0  →  < 10ms
图片查询：每样品单独查 images_xx 分片表
共约 5000 次图片查询（每个样品一次）

结论：✅ DB 无压力，索引已覆盖
```

#### 磁盘 I/O
```
读取：5000张图 × 1MB = 5GB 顺序读取
写入：ZIP 5GB 顺序写入
      SSD：5GB ÷ 500MB/s = 10秒（I/O不是瓶颈）
      HDD：5GB ÷ 100MB/s = 50秒
实际瓶颈在 CPU 缩放（2.5分钟），不在 I/O

结论：✅ I/O 不是瓶颈
```

#### 并发导出（最大风险）
```
场景：2 个人同时点导出
问题：
  - 10000 次图片缩放（5分钟）
  - CPU 满，其他请求变慢
  - 2 个 ZIP 同时写磁盘

对策（必须实施）：
① 导出任务排队（同一时间只运行 1 个）
② 前端显示 "队列位置: 第 X 位"
③ 超时保护（单个导出最多 10 分钟）
```

### 5.3 50并发场景分析（关键）

#### 5.3.1 为什么不能同步执行

```
假设 50 人同时点导出，同步 HTTP 直接处理：

CPU：50 线程同时缩放图片 → 上下文切换开销巨大 → CPU 满载
DB： 50 人 × 多次查询 → 80个连接池瞬间耗尽 → 其他用户页面都打不开
I/O：50个ZIP同时写磁盘 → 随机写入 → 速度暴降 10 倍以上
内存：50 × 50MB峰值 = 2.5GB → OOM

结论：❌ 50并发同步执行必然崩溃
```

#### 5.3.2 也不能排队轮流做

```
排队（一次只跑一个）：
  小任务 50条 × 1.5s → 大任务 2000条 × 60s
  
  50 个人轮流：
  平均等待 = (50/2) × 平均耗时 ≈ 25 × 15s ≈ 6分钟
  最后一个要等：50 × 15s ≈ 12分钟
  
  用户点导出 → 转圈 12 分钟 → 体验极差
```

#### 5.3.3 正确方案：异步任务池

```
┌──────────────────────────────────────────────────────┐
│               异步任务池架构                            │
├──────────────────────────────────────────────────────┤
│                                                      │
│  用户点「导出」                                         │
│      │                                               │
│      ▼                                               │
│  ┌─────────────┐                                     │
│  │  导出 Controller │  ← 立即返回 "任务已提交"          │
│  └──────┬───────┘                                     │
│         │ 提交到队列                                    │
│         ▼                                            │
│  ┌──────────────────────────────┐                    │
│  │      BlockingQueue           │                    │
│  │    (任务排队缓冲区)            │                    │
│  └──────┬───────────────────────┘                    │
│         │                                            │
│         ▼                                            │
│  ┌──────────────────────────────┐                    │
│  │   ThreadPoolExecutor         │                    │
│  │   core=4, max=8              │  ← 4-8个worker     │
│  │   queue=20                   │    同时处理         │
│  └──────┬───────────────────────┘                    │
│         │                                            │
│         ▼                                            │
│  ┌──────────────────────────────┐                    │
│  │  导出文件存到磁盘              │                    │
│  │  D:/photo-management/exports/ │                    │
│  └──────────────────────────────┘                    │
│         │                                            │
│         ▼                                            │
│  前端轮询进度 → 完成后显示下载链接                       │
│                                                      │
└──────────────────────────────────────────────────────┘
```

#### 5.3.4 8 Worker 处理 50 任务的推算

| 任务类型 | 数量 | 单任务耗时 | 8 Workers 完成时间 |
|---------|------|-----------|-------------------|
| 50条（小） | 10个 | ~1.5秒 | **第1轮：1.5秒全部完成** |
| 200条（中） | 20个 | ~6秒 | **第2-3轮：6-12秒完成** |
| 500条（大） | 15个 | ~15秒 | **第4-5轮：15-30秒完成** |
| 2000条（巨） | 5个 | ~60秒 | **第6-8轮：30-60秒完成** |

```
最坏情况（最后一个人，2000条）：
  排队等前面大任务跑完 ≈ 1-2分钟
  自己任务跑 ≈ 1分钟
  总共 ≈ 2-3分钟拿到下载链接
  
平均情况（中等任务，200条）：
  排队 ≈ 10-20秒
  自己跑 ≈ 6秒
  总共 ≈ 15-30秒
```

#### 5.3.5 资源限制分析

| 资源 | 8 Worker 消耗 | 50 任务排队中 | 安全？ |
|------|-------------|-------------|--------|
| **CPU** | 6-8线程缩放图片 ≈ 60-80% | 排队的不占CPU | ✅ | (服务器 6核/12线程) |
| **DB连接** | 8 Worker × 1-2连接 = 16个 | 排队的不占 | ✅ (池80) |
| **内存** | 8 × 50MB = 400MB | 任务元数据 ~1MB | ✅ |
| **磁盘写** | 8个ZIP顺序写 | — | ✅ |
| **磁盘空间** | 8个导出文件 | 完成后提供下载 | ⚠️ 需定期清理 |

```
⚠️ 磁盘空间是唯一需要关注的：
  50人 × 平均400MB = 20GB 导出文件堆积
  对策：文件级缓存 24 小时，到期自动删除，或手动提前清理
```

#### 5.3.6 缓存策略（最终确认）

```
策略：ZIP 文件缓存 24 小时

  ✅ 相同请求（同数据）→ 复用已有 ZIP，秒下
  ✅ 不同请求（数据有变）→ 重新生成，不折腾
  ✅ 超过 24 小时未被复用 → 自动删除
  ✅ 磁盘峰值 ~20GB，安全可控

  不做的：
  ❌ 不做图片级缓存（缩放占比小，收益有限）
  ❌ 不做增量更新（复杂度高，维护成本大）
```

### 5.4 长期使用总结（50并发版）

```
┌──────────────────────────────────────────────────┐
│        长期使用可行性：✅ 可行（需异步架构）         │
├──────────────────────────────────────────────────┤
│                                                   │
│  架构要求（必须）：                                 │
│  🔴 异步任务池（不阻塞HTTP请求）                    │
│  🔴 4-8个Worker线程（平衡CPU和并发）                │
│  🔴 前端进度轮询（不焦虑等待）                       │
│  🔴 导出文件定时清理（防磁盘占满）                    │
│                                                   │
│  无需担心：                                        │
│  ✅ 内存（流式处理 + 限制Worker数）                  │
│  ✅ 数据库（分页查询 + 连接池80足够）                │
│  ✅ CPU（8线程可控，不影响其他请求）                  │
│                                                   │
│  需要监控：                                        │
│  ⚠️ export 目录磁盘使用量                           │
│  ⚠️ 任务队列深度（堆积太多需扩容Worker）             │
│                                                   │
└──────────────────────────────────────────────────┘
```

---

## 六、实施计划

### 第一阶段：核心功能（同步版，快速验证）
- [ ] 后端导出 Controller + Service
  - 流式 ZIP 生成
  - SXSSFWorkbook 流式 Excel
  - 图片缩放嵌入
  - 原图流式写入 ZIP
- [ ] 前端导出按钮 + 下载触发
- [ ] **限制：同一时间只允许 1 个导出**（临时方案）

### 第二阶段：异步架构（支持 50 并发）
- [ ] ThreadPoolExecutor 线程池配置
- [ ] 导出任务提交 → 立即返回任务ID
- [ ] 前端轮询进度接口 `GET /export/progress/{taskId}`
- [ ] 完成后返回下载链接 `GET /export/download/{taskId}`
- [ ] 定时清理过期导出文件（24小时后自动删除）

### 第三阶段：体验优化
- [ ] 前端进度条（百分比 + 已处理条数）
- [ ] 导出历史记录查看
- [ ] 失败自动重试（最多3次）
- [ ] 队列位置预估等待时间

### 第四阶段：高级功能
- [ ] 按条件筛选导出（厂商/种类/时间）
- [ ] 导出列自定义
- [ ] PDF 打印确认表

---

## 七、配置建议

```yaml
# application.yml 新增
app:
  export:
    # === 分页与性能 ===
    db-page-size: 500          # 数据库分页查询每批条数
    worker-count: 8            # 导出Worker线程数（6核/12线程服务器推荐6-8）

    # === 超时控制 ===
    timeout-minutes: 15        # 单任务最长执行时间
    
    # === 文件存储 ===
    output-dir: D:/photo-management/exports   # 导出文件存放目录
    file-retention-hours: 24   # 文件保留时间（超时自动删除）

    # === 图片缩放 ===
    image-scale:
      width: 800
      height: 600
      quality: 0.85
      format: jpeg

# === 线程池（Spring Boot 异步） ===
spring:
  task:
    execution:
      pool:
        core-size: 8           # 导出Worker
        max-size: 12           # 缓冲区
        queue-capacity: 50     # 最多排队50个任务
      thread-name-prefix: export-
```

```bash
# JVM 启动参数（应对 8 Worker + 正常业务请求）
-Xms512m -Xmx1024m -XX:+UseG1GC -XX:MaxGCPauseMillis=200
```

---

## 八、API 设计

### 8.1 提交导出任务

```
POST /api/export
Content-Type: application/json

请求体:
{
  "keyword": "可选筛选关键词",
  "sampleIds": [1,2,3]  // 可选，指定导出哪些样品（不传则全量）
}

响应:
{
  "code": 200,
  "data": {
    "taskId": "abc123-xxx",
    "message": "导出任务已提交，预计 2 分钟后完成"
  }
}
```

### 8.2 查询进度

```
GET /api/export/progress/{taskId}

响应:
{
  "code": 200,
  "data": {
    "status": "PROCESSING",    // PENDING | PROCESSING | COMPLETED | FAILED
    "totalRecords": 5000,
    "processedRecords": 3200,
    "percentage": 64,
    "queuePosition": 3,        // 前面还有几个任务
    "estimatedSeconds": 45     // 预估剩余秒数
  }
}
```

### 8.3 下载文件

```
GET /api/export/download/{taskId}

响应:
Content-Type: application/zip
Content-Disposition: attachment; filename="export_20260521_143000.zip"
↓↓↓ 流式输出 ZIP 文件 ↓↓↓
```

---

*文档版本: v2.0 | 日期: 2026-05-21 | 更新: 50并发异步架构 + 24小时缓存策略*
