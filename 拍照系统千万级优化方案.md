# 拍照管理系统千万级优化方案（修正版）

## 一、架构现状

| 维度 | 当前状态 | 说明 |
|------|---------|------|
| 数据库 | MySQL 8.0 + 图片表256分片 | ✅ 已完成 |
| 缓存 | Redis 单实例 | ✅ 代码就绪，待安装Redis |
| 文件存储 | 本地 hash 分目录 | ✅ 已是最优方案 |
| 前端 | Nginx 静态托管 | ✅ 配置就绪 |
| 部署 | 单节点 Spring Boot | 当前足够 |

## 二、已完成优化

### P0：图片表分表（2026-05-18）

- `images` 按 SHA256 hash 首2位分为 256 张子表：`images_00` ~ `images_ff`
- MyBatis Plus `DynamicTableNameInnerInterceptor` + `ThreadLocal` 路由
- 单表数据均匀分布，3000w ÷ 256 ≈ 12w/表，查询永远毫秒级
- 文件存储同步 hash 分目录：`D:/photo-management/images/0a/1b/hash.jpg`

### P0：索引优化（2026-05-18）

新增 8 个复合索引：

| 表 | 索引 | 场景 |
|----|------|------|
| images | `idx_gallery_sort` | 图库图片列表排序 |
| images | `idx_gallery_create` | 图库图片按时间查 |
| chat_messages | `idx_conv_time` | 会话消息分页 |
| samples | `ftx_sample_search` | 全文搜索 |
| users | `idx_real_name` | 搜索用户 |
| gallery | `idx_category_time` | 图库分类查询 |
| login_logs | `idx_user_time` | 登录日志查询 |
| operation_logs | `idx_module_time` | 操作日志查询 |

### P1：Redis 缓存（代码就绪）

集成 `spring-boot-starter-data-redis` + Spring Cache：

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis
    redis:
      time-to-live: 600000  # 10分钟
```

缓存策略：
- `@Cacheable("roles")` — 角色详情
- `@Cacheable("all_permissions")` — 权限列表
- `@CacheEvict("roles")` — 角色变更时失效

### P1：Nginx 静态文件（配置就绪）

`backend/nginx.conf`：
- 80 端口访问前端，`/api/` 反向代理到 Spring Boot 8080
- 静态资源 30 天强缓存
- gzip 压缩 CSS/JS
- 图片请求代理到后端，客户端缓存 30 天

## 三、为什么不用 MinIO

| | MinIO | 当前 Hash 分目录 |
|----|------|-----|
| 单机部署 | 额外 HTTP 调用层 | 直接文件 IO |
| 部署复杂度 | 额外进程 | 零依赖 |
| 运维 | 需单独监控 | 不操心 |
| 适用场景 | 多服务器共享 | **本地部署** ✅ |

结论：本地部署 Hash 分目录已经是最优解。

## 四、后续可做（按需）

| 优先级 | 内容 | 触发条件 |
|--------|------|----------|
| P1 | Redis 安装启动 | 立即可做 |
| P1 | Nginx 部署 | 静态文件多时 |
| P2 | MySQL 读写分离 | 单表 > 500万行 |
| P2 | 消息队列（RabbitMQ） | 批量缩略图异步生成 |
| P3 | Docker + K8s | 运维团队到位后 |
| P3 | SkyWalking / ELK | 排查性能瓶颈时 |

## 五、性能预期

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 图片单表数据量 | 3000w | 12w（256分片） |
| 图片上传 | O(n) 全表扫描去重 | O(1) 单分片查询 |
| 角色权限查询 | 每次查DB | Redis 毫秒命中 |
| 静态文件 | Spring Boot 托管 | Nginx sendfile 零拷贝 |
| 前端首屏 | 每次下载 | 30天强缓存 |
