# 图像搜索升级方案

## 目标

1. 截图搜索精度达到 90%+
2. 200 万张图片规模下毫秒级检索

## 当前问题

- 手工特征（4681 维直方图）区分度不足，同类产品相似度挤在窄带
- MySQL 分表扫特征做余弦比较，200 万张不可接受
- 截图与原图的特征差异天然偏大（浏览器渲染、压缩、裁剪）

---

## 阶段一：MobileNet 替换手工特征

### 改动

| 文件 | 改动 |
|------|------|
| `pom.xml` | 添加 DJL 依赖 |
| `FeatureExtractor.java` | 新增 `extractDeepFeature()`，保留旧方法 |
| `ImageService.java` | 搜索入口增加开关 |
| `application.yml` | 加配置项 |

### DJL 依赖

```xml
<dependency>
    <groupId>ai.djl.pytorch</groupId>
    <artifactId>djl-pytorch-engine</artifactId>
    <version>0.31.0</version>
</dependency>
<dependency>
    <groupId>ai.djl.mxnet</groupId>
    <artifactId>djl-mxnet-engine</artifactId>
    <version>0.31.0</version>
</dependency>
```

### 特征对比

| | 当前 | MobileNet |
|------|------|-----------|
| 维度 | 4681 | 1280 |
| 提取速度（CPU） | 5ms | ~50ms |
| 同类产品区分度 | 挤在 79-87% | 85-95% |
| 截图 vs 原图 | 0.79 | 预计 0.85-0.90 |

### 开关逻辑

```
search.image.engine=deep   → 走 MobileNet
search.image.engine=legacy → 走旧特征（兜底）
```

---

## 阶段二：pgvector 向量数据库

### 环境搭建

```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE image_vectors (
    image_id BIGINT PRIMARY KEY,
    embedding VECTOR(1280),
    FOREIGN KEY (image_id) REFERENCES images(id)
);

CREATE INDEX ON image_vectors USING ivfflat (embedding vector_cosine_ops) WITH (lists = 2000);
```

### 改动

| 文件 | 改动 |
|------|------|
| `pom.xml` | 加 pgvector JDBC 支持 |
| `application.yml` | pgvector 连接配置 |
| `ImageService.java` | 搜索方法改为 pgvector ANN 查询 |
| `backfillFeatures()` | 双写（MySQL + pgvector） |

### 搜索性能

| | MySQL 扫表 | pgvector ANN |
|------|-----------|-------------|
| 1 万张 | ~200ms | ~5ms |
| 10 万张 | ~2s | ~10ms |
| 200 万张 | 不可接受 | ~30ms |

---

## 阶段三：截图预处理优化

### 当前问题

- 截图有浏览器色彩管理，原图是相机 sRGB
- 截图有 JPEG 压缩伪影
- 截图分辨率通常小于原图

### 改进

1. 色彩空间统一：截图检测后强制转 sRGB，匹配原图色彩空间
2. 抗压缩伪影：高斯模糊 0.5px 预处理，消除高频压缩噪声
3. 分辨率增强：小截图用双三次插值上采样到 224x224

---

## 阶段四：Siamese 微调

### 目标

用现有产品数据训练专用匹配模型，达到 90%+ 精度。这是完整的实施步骤，不跟阶段一分开考虑。

### 训练数据生成（自动，无需人工标注）

```
原图 → 截图模拟 → 正样本对（同产品 label=1）
原图 → 其他产品 → 负样本对（不同产品 label=0）
```

每张原图自动生成 5-10 个模拟截图，不需要人工介入。

| 模拟操作 | 参数 | 目的 |
|---------|------|------|
| 浏览器 UI 边框中出 | 上下留白 8-15% | 模拟截图裁剪后状况 |
| JPEG 压缩 | 质量 50-80% | 模拟传输损耗 |
| 缩放 | 0.4x-0.8x | 模拟不同分辨率 |
| 色彩偏移 | ±5% 色温/饱和度 | 模拟显示差别 |
| 轻微旋转/透视 | 0°-3° | 模拟手机截屏角度 |

### 模型设计

```
输入的截图 → MobileNet → 128维向量 → ┐
                                     ├→ cosine_distance → 损失函数
输入的原图 → MobileNet → 128维向量 → ┘
```

关键点：
- 两张图走同一个 MobileNet（共享权重），不是两个独立模型
- 微调后输出 `128 维` 而非 1280 维，pgvector 索引效率更高
- 训练完成后只保留 MobileNet 权重文件（.pt），替换阶段一的 DJL 模型

### 训练环境

| 项目 | 要求 |
|------|------|
| 框架 | PyTorch（Python） |
| GPU | 建议一张 NVIDIA，CPU 也能练但慢 |
| 训练时长 | GPU 4-8 小时 / 10 万样本 |
| 输出 | MobileNet 权重文件（~12MB） |

### 推理（Java 端）

```java
Model model = Model.newInstance("mobile-v3-small-siamese");  // 加载微调权重
model.load(Paths.get("siamese_mobilenet.pt"));
float[] features = model.predict(image);  // 128 维向量
```

阶段一 DJL 代码不变，只换权重文件。

---
## 完整精度路线

```
当前手工特征      ~80%    ← 同类产品挤在窄带
  └→ 加 MobileNet  ~88%   ← 基础深度特征，主力提升
      └→ 加预处理   ~90%   ← 抹平截图/原图差异
          └→ 加 Siamese ~93%   ← 用你的产品数据微调，到顶
```

---

## 部署策略

### 变动对整个系统的影响

| 功能模块 | 是否受影响 |
|----------|-----------|
| 样品管理 CRUD | 不受影响 |
| 图片上传/删除 | 上传时多写一条 pgvector |
| 前端界面 | 不受影响（API 签名不变） |
| 图片存储 | 不受影响 |
| 历史数据 | 需要一次全量 backfill |

### 灰度方案

```
搜索入口
  ├── search.engine=legacy → MySQL + 旧特征（一键回退）
  └── search.engine=deep   → pgvector + MobileNet
```

backfill 分批进行，每批 5000 张，不影响在线服务。

---

## 实施顺序

| 顺序 | 步骤 | 预计效果 | 风险 |
|------|------|---------|------|
| 1 | 阶段一 MobileNet | 精度 80% → 88% | 低，有开关兜底 |
| 2 | 阶段二 pgvector | 速度从扫表 → 毫秒 | 中，需装 PG 扩展 |
| 3 | 阶段三 截图预处理 | 精度 88% → 90% | 低 |
| 4 | 阶段四 Siamese | 精度 90% → 93% | 中，需 GPU 训练 |

---

## 预估资源

| 项目 | 当前 | 升级后 |
|------|------|--------|
| 硬盘 | ~100MB（特征） | ~3GB（1280维 × 200万 × 4字节） |
| CPU（搜索） | 高（全表扫） | 低（ANN 索引） |
| CPU（backfill 200 万张） | — | ~30 小时（渐进，不阻塞） |
| 内存 | 不变 | 不变 |
