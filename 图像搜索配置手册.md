# 图像搜索功能 — 手动配置手册

> 本文档列出图像搜索功能所有需要手动配置的项目，适用于首次部署或从零搭建环境。

---

## 一、环境要求

| 组件 | 最低版本 | 说明 |
|------|---------|------|
| PostgreSQL | 15+ | 向量数据库（pgvector 插件） |
| pgvector | 0.8.0+ | PostgreSQL 向量扩展 |
| PyTorch (native) | 2.0+ | DJL 引擎所需的本地动态库 |
| 磁盘空间 | 模型文件 ~9MB，向量索引随数据增长 | — |

---

## 二、PostgreSQL + pgvector 安装配置

### 2.1 安装 pgvector 扩展

**方式一：使用官方安装包**

- 下载地址：https://github.com/pgvector/pgvector
- 编译安装后执行：

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

**方式二：使用 Windows 预编译包**

项目中附带 `pgvector/` 目录，包含预编译的 `vector.dll`。

1. 将 `vector.dll` 复制到 PostgreSQL 安装目录的 `lib/` 下
2. 将 `vector.control` 和 SQL 文件复制到 `share/extension/` 下
3. 重启 PostgreSQL 服务

### 2.2 创建数据库和向量表

```sql
-- 1. 创建数据库（如果不存在）
CREATE DATABASE photo_management WITH ENCODING 'UTF8';

-- 2. 连接数据库后创建扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- 3. 创建向量表（注意：必须包含 shard_prefix 列）
CREATE TABLE IF NOT EXISTS image_vectors (
    image_id BIGINT NOT NULL,
    shard_prefix VARCHAR(2) NOT NULL,
    embedding VECTOR(1280),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (image_id, shard_prefix)
);

-- 4. 创建 IVFFlat 索引（余弦相似度）
-- 注意：需要在表中插入一定数据后再创建索引才能生效
CREATE INDEX IF NOT EXISTS idx_image_vectors_embedding
    ON image_vectors USING ivfflat (embedding vector_cosine_ops)
    WITH (lists = 200);
```

**参数说明：**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `VECTOR(1280)` | 1280 | 向量维度，必须与 MobileNetV2 输出维度一致 |
| `PRIMARY KEY (image_id, shard_prefix)` | — | 复合主键，image_id 来自 MySQL images 分片表 |
| `lists = 200` | 200 | IVFFlat 索引分桶数。建议值为 sqrt(行数)，1000万数据建议设为 3000 |

### 2.3 配置 PostgreSQL 连接信息

编辑 `application.yml`：

```yaml
pgvector:
  enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/photo_management
    username: postgres
    password: your_password_here          # ← 修改为实际密码
```

**注意事项：**

- `pgvector.enabled` 设为 `false` 可关闭向量搜索（回退到全表扫描 + MySQL 特征比对）
- 密码不要包含特殊字符 `#` `&` 等（URL 编码问题）
- 默认连接池大小 20，可在 `PgVectorService.java` 第 52 行调整

---

## 三、MySQL 分片表配置

### 3.1 添加 deep_feature_vector 列

系统使用 256 张 `images_00` ~ `images_ff` 分片表。需在每张表上添加深度特征列：

```sql
-- 方式一：执行批量脚本
source backend/sql/add-deep-feature-column.sql;

-- 方式二：手动执行
ALTER TABLE images_00 ADD COLUMN deep_feature_vector MEDIUMBLOB DEFAULT NULL AFTER feature_vector;
-- ... 共 256 张表
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `deep_feature_vector` | MEDIUMBLOB | MobileNetV2 1280 维浮点特征（5120 字节），存储为 float 数组字节序列 |

### 3.2 数据库连接配置

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/photo_management?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: your_password_here          # ← 修改为实际密码
    hikari:
      maximum-pool-size: 80
      minimum-idle: 10
      connection-timeout: 30000
```

---

## 四、深度学习模型部署

### 4.1 模型文件

- **模型格式**：TorchScript (.pt)
- **模型架构**：MobileNetV2（特征提取，去除分类头）
- **输入尺寸**：224×224×3（RGB）
- **输出维度**：1280（全局平均池化后特征）
- **文件大小**：约 9MB
- **文件位置**：`d:/models/mobilenet_v2_feat/mobilenet_v2_feat.pt`（推荐）

### 4.2 模型搜索路径优先级

系统按以下顺序查找模型文件：

| 优先级 | 路径 | 说明 |
|--------|------|------|
| 1 | `d:/models/mobilenet_v2_feat/mobilenet_v2_feat.pt` | **硬编码绝对路径（Windows 专用）** |
| 2 | `${search.image.siamese-model-path}` | application.yml 配置路径 |
| 3 | `backend/models/mobilenet_v2_feat/mobilenet_v2_feat.pt` | 项目相对路径 |
| 4 | `models/mobilenet_v2_feat/mobilenet_v2_feat.pt` | 工作目录相对路径 |
| 5 | `../models/mobilenet_v2_feat/mobilenet_v2_feat.pt` | 父目录相对路径 |
| 6 | `d:/客户端测试/backend/models/mobilenet_v2_feat/mobilenet_v2_feat.pt` | 中文路径备用（仅 Windows） |

**注意事项：**

- **Linux 部署必须将模型放在配置路径或相对路径中**，因为优先级 1 和 6 是 Windows 绝对路径
- 推荐在 `application.yml` 中配置 `search.image.siamese-model-path` 指向模型文件
- DJL PyTorch 引擎对**中文路径兼容性差**（native 库 `fopen` 编码问题），请使用纯英文路径

### 4.3 模型训练/导出（如有自定义模型）

```bash
# 在 training/ 目录下
pip install -r requirements.txt

# 1. 生成训练样本对
python generate_pairs.py

# 2. 训练孪生网络
python train_siamese.py

# 3. 导出 TorchScript 模型
python export_model.py
```

---

## 五、搜索引擎配置

### 5.1 application.yml 核心配置

```yaml
search:
  image:
    engine: deep                          # 搜索引擎：legacy | deep
    siamese-model-path: models/mobilenet_v2_feat/mobilenet_v2_feat.pt
```

| 配置项 | 可选值 | 默认值 | 说明 |
|--------|--------|--------|------|
| `search.image.engine` | `legacy` / `deep` | `legacy` | `legacy`=传统手工特征（边缘+颜色+LBP）；`deep`=MobileNetV2 深度学习 |
| `search.image.siamese-model-path` | 文件路径 | 空 | 深度学习模型路径。仅 `engine=deep` 时生效 |

### 5.2 生产环境配置（application-prod.yml）

生产环境默认禁用深度学习搜索和 pgvector：

```yaml
search:
  image:
    engine: legacy
    siamese-model-path:

pgvector:
  enabled: false
```

启用时修改为与开发环境相同的配置即可。

---

## 六、图片上传路径配置

```yaml
app:
  upload:
    path: d:/客户端测试/photo-management/uploads        # 上传根目录
    image-path: d:/客户端测试/photo-management/images    # 原图存储路径
    thumbnail-path: d:/客户端测试/photo-management/thumbnails  # 缩略图路径
    video-path: d:/客户端测试/photo-management/videos    # 视频存储路径
    ffmpeg-path: ffmpeg                                  # FFmpeg 可执行文件路径
```

**注意事项：**

- 所有路径必须存在且有写入权限，服务不会自动创建目录
- 图片文件按日期和 UUID 命名，需确保磁盘空间充足
- 删除图片时不会自动清理磁盘文件，需手动或脚本定期清理

---

## 七、Nginx 反向代理配置（推荐）

以下配置用于生产环境，将图片请求直接由 Nginx 处理（bypass Java 后端）：

```nginx
upstream backend {
    server 127.0.0.1:8080;
}

server {
    listen 80;
    server_name your-domain.com;

    # API 和前端代理到后端
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 图片资源直接由 Nginx 提供
    location /images/ {
        alias D:/photo-management/uploads/images/;
        expires 30d;
    }

    # 缩略图直接由 Nginx 提供
    location /thumbnails/ {
        alias D:/photo-management/uploads/thumbnails/;
        expires 30d;
    }
}
```

---

## 八、首次启动后的初始化步骤

部署完成后，需按以下顺序执行初始化：

### 8.1 启动服务

```bash
java -jar app.jar --spring.profiles.active=prod
```

### 8.2 回填 dHash（传统哈希搜索基础）

```
POST /images/backfill-dhash
```

为所有已存在的图片计算差异哈希值，这是搜索的基础索引。

### 8.3 回填深度特征（MySQL）

```
POST /images/backfill-deep-features
```

使用 MobileNetV2 模型为所有已存在的图片提取 1280 维特征，存入 MySQL 分片表的 `deep_feature_vector` 列。

**前提条件：**
- `search.image.engine=deep`
- 模型文件已部署在可访问路径
- DJL PyTorch 引擎可用

### 8.4 回填 pgvector 向量库

```
POST /images/backfill-pgvector
```

将 MySQL 中的深度特征同步到 PostgreSQL pgvector 表中。

**前提条件：**
- `pgvector.enabled=true`
- PostgreSQL + pgvector 扩展已正确安装
- 已完成深度特征回填（上一步）

### 8.5 验证状态

```
GET /images/pgvector-status
```

返回 pgvector 可用性和向量记录数。

---

## 九、API 端点汇总

| 端点 | 方法 | 说明 |
|------|------|------|
| `/images/search-by-image` | POST | 图像搜索（multipart file + maxDistance 参数） |
| `/images/backfill-dhash` | POST | 回填所有图片的 dHash |
| `/images/backfill-features` | POST | 回填所有图片的传统特征 |
| `/images/backfill-deep-features` | POST | 回填所有图片的 MobileNetV2 深度特征 |
| `/images/backfill-pgvector` | POST | 将深度特征同步到 pgvector |
| `/images/reset-features` | POST | 清空所有特征（dhash + 传统 + 深度） |
| `/images/pgvector-status` | GET | 获取 pgvector 状态 |

---

## 十、常见问题排查

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 模型加载失败 `ModelNotFoundException` | 模型文件不在搜索路径中 | 检查模型文件路径，确认使用纯英文路径 |
| `open file failed because of errno 2` | 中文路径编码问题 | 将模型复制到纯英文路径（如 `d:/models/`） |
| pgvector 不可用 | PostgreSQL 未安装 pgvector 扩展 | 执行 `CREATE EXTENSION vector;` |
| 搜索结果相似度偏低 | 特征未回填或模型不匹配 | 执行 reset-features 后重新回填全链路 |
| 搜索耗时过长 | pgvector 未启用，走全表扫描 | 检查 `pgvector.enabled=true` 并确认 pgvector 可用 |
| `ON CONFLICT` 语法报错 | pgvector 表缺少 shard_prefix 列 | 重建表，使用本文档第 2.2 节的建表语句 |
| Redis 连接超时 | Redis 未启动或配置错误 | 确认 `spring.data.redis.host` 配置正确 |

---

## 十一、安全注意事项

1. **数据库密码**：`application.yml` 中明文存储，生产环境建议使用环境变量或外部配置中心
2. **JWT 密钥**：生产环境务必修改 `app.jwt.secret`，使用高强度随机字符串（至少 256 位）
3. **上传限制**：`spring.servlet.multipart.max-file-size` 当前为 50MB，根据需要调整
4. **pgvector 连接**：默认无 SSL，生产环境建议启用 SSL 连接
5. **模型文件**：TorchScript 模型文件包含完整模型权重，不应对外公开
