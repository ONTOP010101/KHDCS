# 拍照管理系统 — 完整服务器部署指南

## 一、服务器环境需求总览

| 软件 | 最低版本 | 推荐版本 | 用途 | 是否必需 |
|------|---------|----------|------|----------|
| JDK | 17 | 21 LTS | 运行 Spring Boot 后端 | 必需 |
| MySQL | 8.0 | 8.0.36+ | 数据库（256 分表） | 必需 |
| Maven | 3.8 | 3.9.x | 编译打包后端 | 必需* |
| Node.js | 18 | 20 LTS | 编译前端 | 必需 |
| Redis | 5.0 | 7.x | 缓存 | 必需 |
| FFmpeg | 6.x | 7.x | 视频压缩处理 | 必需 |
| Nginx | 1.24 | 1.26+ | 反向代理 + 静态资源 | 推荐 |
| Git | 2.x | 2.43+ | 拉取代码 | 推荐 |

> * 必需：可以在开发机上打好 jar 包再上传到服务器，这样服务器就不需要安装 Maven。

---

## 二、软件下载地址及安装步骤

### 2.1 JDK（Java Development Kit）

| 项目 | 内容 |
|------|------|
| **下载地址** | https://adoptium.net/temurin/releases/?os=windows&arch=x64&package=jdk&version=21 |
| **推荐版本** | Eclipse Temurin JDK 21 LTS (x64) |
| **安装方式** | 下载 `.msi` 安装包，双击安装即可 |
| **安装路径** | 默认 `C:\Program Files\Eclipse Adoptium\jdk-21.x.x-hotspot` |
| **验证命令** | `java -version` |

**注意事项：**
- 必须安装 JDK 17 或以上（项目 Spring Boot 3.2.5 要求 Java 17+）
- 推荐使用 Eclipse Temurin（免费、开源、长期支持）
- 安装时勾选「Add to PATH」，会自动配置环境变量

### 2.2 MySQL 8.0

| 项目 | 内容 |
|------|------|
| **下载地址** | https://dev.mysql.com/downloads/windows/installer/8.0.html |
| **推荐版本** | mysql-installer-community-8.0.36.0.msi（约 400MB） |
| **轻量版** | mysql-installer-web-community-8.0.36.0.msi（约 2MB，在线安装） |
| **安装方式** | 双击 `.msi` 安装 |

**安装步骤要点：**
1. 选择 **Server only** 或 **Developer Default**
2. 配置 root 密码（建议与 `application.yml` 中的 `123456` 一致，或部署后修改配置）
3. 将 MySQL 注册为 Windows 服务（默认勾选）
4. 字符集选择 **utf8mb4**（项目使用 utf8mb4_unicode_ci）

**安装后验证：**
```powershell
mysql -u root -p
# 输入密码后能进入 mysql> 提示符
```

**配置优化建议**（修改 `my.ini`）：
```ini
[mysqld]
max_connections = 1000
innodb_buffer_pool_size = 2G
innodb_log_file_size = 256M
max_allowed_packet = 100M
```

### 2.3 Node.js

| 项目 | 内容 |
|------|------|
| **下载地址** | https://nodejs.org/en/download/ |
| **推荐版本** | Node.js 20 LTS（长期支持版） |
| **安装方式** | 下载 `.msi` 安装包 |

**安装时注意：**
- 勾选「Add to PATH」
- 勾选「Automatically install necessary tools」（可选）

**安装后验证：**
```powershell
node -v    # 应显示 v20.x.x
npm -v     # 应显示 10.x.x
```

### 2.4 Maven

| 项目 | 内容 |
|------|------|
| **下载地址** | https://maven.apache.org/download.cgi |
| **推荐版本** | Apache Maven 3.9.6（Binary zip） |
| **直接下载（官方归档）** | https://archive.apache.org/dist/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.zip |
| **直接下载（国内镜像，推荐）** | https://mirrors.aliyun.com/apache/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.zip |

**安装步骤：**
1. 解压到 `C:\Program Files\Apache\maven`
2. 添加环境变量：
   - 新建 `MAVEN_HOME` = `C:\Program Files\Apache\maven`
   - 在 `Path` 中添加 `%MAVEN_HOME%\bin`
3. 修改 `conf\settings.xml`，配置国内镜像加速：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
    <mirrorOf>central</mirrorOf>
  </mirror>
</mirrors>
```

**安装后验证：**
```powershell
mvn -version
```

### 2.5 Redis

| 项目 | 内容 |
|------|------|
| **Windows 版下载** | https://github.com/tporadowski/redis/releases/download/v5.0.14.1/Redis-x64-5.0.14.1.zip |
| **推荐方案（Docker）** | 如果服务器支持 Docker，推荐使用 Redis 7 官方镜像 |

**安装步骤（原生）：**
1. 解压到 `C:\Redis`
2. 以管理员身份打开 PowerShell，注册为服务：

```powershell
C:\Redis\redis-server.exe --service-install --service-name Redis --port 6379
net start Redis
```

**安装后验证：**
```powershell
C:\Redis\redis-cli.exe ping
# 应返回 PONG
```

### 2.6 FFmpeg

| 项目 | 内容 |
|------|------|
| **下载地址** | https://www.gyan.dev/ffmpeg/builds/ |
| **推荐版本** | ffmpeg-release-essentials.7z（精简版） |
| **直接下载** | https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.7z |

**安装步骤：**
1. 解压到 `C:\ffmpeg`
2. 添加环境变量：在 `Path` 中添加 `C:\ffmpeg\bin`

**安装后验证：**
```powershell
ffmpeg -version
# 应显示版本信息
```

> 项目中的视频上传功能使用 FFmpeg 进行视频压缩处理，必须安装。

### 2.7 Nginx（推荐）

| 项目 | 内容 |
|------|------|
| **下载地址** | https://nginx.org/en/download.html |
| **推荐版本** | nginx/Windows-1.26.3（稳定版） |
| **直接下载** | https://nginx.org/download/nginx-1.26.3.zip |

**安装步骤：**
1. 解压到 `C:\nginx`
2. 使用项目自带的 `nginx.conf` 配置（见下文 部署流程）

**启动命令：**
```powershell
cd C:\nginx
nginx.exe
```

---

## 三、从 GitHub 拉取代码

如果服务器上安装了 Git，可以直接从仓库拉取：

```powershell
# 安装 Git
# 下载：https://git-scm.com/download/win

# 拉取代码
cd D:\
git clone https://github.com/ONTOP010101/KHDCS.git photo-management
cd photo-management
```

---

## 四、完整部署流程

### 4.1 开发机上准备部署包（方式 A：推荐）

在开发机上操作，打好包后整个文件夹上传到服务器。

**步骤 1：编译前端**
```powershell
cd frontend
npm install
npm run build
# 产物在 frontend/dist/ 目录
```

**步骤 2：复制前端产物到后端静态资源目录**
```powershell
Remove-Item -Recurse -Force ..\backend\src\main\resources\static\*
Copy-Item -Recurse dist\* ..\backend\src\main\resources\static\
```

**步骤 3：编译打包后端**
```powershell
cd ..\backend
mvn clean package -DskipTests
# 产物：backend\target\photo-management-1.0.0.jar
```

**步骤 4：准备部署目录**
```powershell
# 创建部署目录
mkdir D:\photo-management-deploy
mkdir D:\photo-management-deploy\sql
mkdir D:\photo-management-deploy\uploads
mkdir D:\photo-management-deploy\images
mkdir D:\photo-management-deploy\thumbnails
mkdir D:\photo-management-deploy\logs

# 复制 jar 包
Copy-Item "d:\客户端测试\backend\target\photo-management-1.0.0.jar" "D:\photo-management-deploy\"

# 复制 SQL 脚本
Copy-Item "d:\客户端测试\backend\sql\*.sql" "D:\photo-management-deploy\sql\"

# 复制配置文件
Copy-Item "d:\客户端测试\backend\src\main\resources\application.yml" "D:\photo-management-deploy\"
```

**步骤 5：整个部署目录上传到服务器**

---

### 4.2 服务器上操作（方式 A 和方式 B 通用）

**步骤 1：初始化数据库**

```powershell
cd D:\photo-management-deploy

# 按顺序执行 SQL
mysql -u root -p < sql\init.sql
mysql -u root -p < sql\add_indexes.sql
mysql -u root -p < sql\add-dhash-column.sql
mysql -u root -p < sql\add-dhash-index.sql
mysql -u root -p < sql\migrate-samples-49cols.sql
mysql -u root -p < sql\migrate_thumbnail.sql
mysql -u root -p < sql\populate_thumbnail.sql
mysql -u root -p < sql\add-package-code.sql
mysql -u root -p < sql\add_sample_id_index.sql
mysql -u root -p < sql\widen-short-varchar-columns.sql
mysql -u root -p < sql\index-optimization.sql
```

> 也可以合并为一个命令：
> ```powershell
> Get-ChildItem sql\*.sql | ForEach-Object { mysql -u root -p < $_.FullName }
> ```

**步骤 2：修改数据库密码（如果与默认不同）**

编辑 `application.yml`，修改：
```yaml
spring:
  datasource:
    password: 你的实际密码
```

同时修改图片路径：
```yaml
app:
  upload:
    path: D:/photo-management/uploads
    image-path: D:/photo-management/images
    thumbnail-path: D:/photo-management/thumbnails
    video-path: D:/photo-management/videos
```

**步骤 3：导入已有的图片数据**

如果有已有的图片数据（`images/`、`thumbnails/` 目录），复制到对应路径。

**步骤 4：启动后端**

```powershell
# 方式一：直接运行（前台）
java -jar D:\photo-management-deploy\photo-management-1.0.0.jar

# 方式二：后台运行
Start-Process java -ArgumentList "-jar","D:\photo-management-deploy\photo-management-1.0.0.jar" -WindowStyle Hidden
```

**方式三：注册为 Windows 服务（推荐生产环境）**

下载 [WinSW](https://github.com/winsw/winsw/releases)：
```powershell
# 下载 WinSW
Invoke-WebRequest -Uri "https://github.com/winsw/winsw/releases/download/v3.0.0-alpha.11/WinSW-x64.exe" -OutFile "D:\photo-management-deploy\photo-management.exe"

# 创建配置文件 photo-management.xml
```

`photo-management.xml` 内容：
```xml
<service>
  <id>PhotoManagementBackend</id>
  <name>拍照管理系统后端</name>
  <description>Photo Management System Backend Service</description>
  <executable>java</executable>
  <arguments>-jar D:\photo-management-deploy\photo-management-1.0.0.jar</arguments>
  <log mode="roll-by-size">
    <sizeThreshold>10240</sizeThreshold>
    <keepFiles>5</keepFiles>
  </log>
  <workingdirectory>D:\photo-management-deploy</workingdirectory>
</service>
```

注册并启动服务：
```powershell
.\photo-management.exe install
net start PhotoManagementBackend
```

**步骤 5：配置 Nginx 反向代理**

编辑 `C:\nginx\conf\nginx.conf`：

```nginx
worker_processes auto;

events {
    worker_connections 4096;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    tcp_nopush    on;
    keepalive_timeout 65;

    # Gzip 压缩
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 5;
    gzip_types text/css application/javascript application/json image/svg+xml;

    # 后端上游
    upstream backend {
        server 127.0.0.1:8080;
        keepalive 32;
    }

    server {
        listen 80;
        server_name _;

        # 前端静态文件
        location / {
            root D:/photo-management-deploy/static;
            try_files $uri $uri/ /index.html;
            expires 7d;
            add_header Cache-Control "public";
        }

        # API 代理到后端
        location /api/ {
            proxy_pass http://backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 图片资源
        location /images/ {
            alias D:/photo-management-deploy/images/;
            expires 30d;
        }

        location /thumbnails/ {
            alias D:/photo-management-deploy/thumbnails/;
            expires 30d;
        }

        # 大文件上传（超时和大小限制）
        location /samples/upload/ {
            proxy_pass http://backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            client_max_body_size 100m;
            proxy_read_timeout 300s;
        }

        # 图像搜索
        location /images/ {
            proxy_pass http://backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 120s;
        }
    }
}
```

启动 Nginx：
```powershell
cd C:\nginx
.\nginx.exe
```

---

## 五、服务器端完整编译方式（方式 B）

如果服务器安装了 Git + Maven + Node.js，可以直接在服务器上编译。

```powershell
# 1. 克隆仓库
cd D:\
git clone https://github.com/ONTOP010101/KHDCS.git photo-management
cd photo-management

# 2. 编译前端
cd frontend
npm install
npm run build

# 3. 复制前端产物
Remove-Item -Recurse -Force ..\backend\src\main\resources\static\*
Copy-Item -Recurse dist\* ..\backend\src\main\resources\static\

# 4. 编译后端
cd ..\backend
mvn clean package -DskipTests

# 5. 启动
java -jar target\photo-management-1.0.0.jar
```

---

## 六、访问与验证

### 6.1 访问地址

| 访问方式 | 地址 |
|----------|------|
| 本机 | http://localhost:8080 |
| 局域网 | http://服务器IP:8080 |
| Nginx 代理 | http://服务器IP （80 端口）|

### 6.2 默认登录账号

```
用户名: admin
密码:   admin123
```

### 6.3 验证清单

| 检查项 | 验证方法 |
|--------|----------|
| 登录 | admin / admin123 能否正常登录 |
| 用户管理 | 能看到用户列表 |
| 样品列表 | 分页加载是否正常 |
| 图片搜索 | 上传截图能否搜索到结果 |
| 图片上传 | 上传图片后 `images/` 目录是否有文件 |
| Redis 缓存 | `redis-cli keys "*"` 有缓存数据 |

---

## 七、防火墙配置

```powershell
# 允许 8080 端口（后端直连访问）
netsh advfirewall firewall add rule name="Photo Backend 8080" dir=in action=allow protocol=TCP localport=8080

# 允许 80 端口（Nginx 代理访问）
netsh advfirewall firewall add rule name="Nginx 80" dir=in action=allow protocol=TCP localport=80

# 允许 3000 端口（如果单独启用了 Vite 开发服务器）
netsh advfirewall firewall add rule name="Vite 3000" dir=in action=allow protocol=TCP localport=3000
```

---

## 八、开机自启动

### 8.1 MySQL / Redis
安装时通常已自动注册为 Windows 服务，会自启动。

### 8.2 Nginx 自启动

下载 [NSSM](https://nssm.cc/download) 或使用计划任务：

```powershell
# 使用计划任务
$action = New-ScheduledTaskAction -Execute "C:\nginx\nginx.exe" -WorkingDirectory "C:\nginx"
$trigger = New-ScheduledTaskTrigger -AtStartup
$principal = New-ScheduledTaskPrincipal -UserId "SYSTEM" -LogonType ServiceAccount -RunLevel Highest
Register-ScheduledTask -TaskName "Nginx" -Action $action -Trigger $trigger -Principal $principal
```

### 8.3 后端自启动
按上文 4.2 步骤 4 使用 WinSW 注册为服务即可自启。

---

## 九、日常运维

```powershell
# 检查后端服务
tasklist | findstr java

# 检查 Redis
redis-cli ping

# 检查 MySQL
net start | findstr MySQL

# 重启后端（如果用 WinSW 服务）
net stop PhotoManagementBackend
net start PhotoManagementBackend

# 重启后端（如果手动运行）
taskkill /f /im java.exe
java -jar D:\photo-management-deploy\photo-management-1.0.0.jar

# 查看后端日志（WinSW）
Get-Content D:\photo-management-deploy\photo-management.out.log -Tail 50

# Nginx 操作
cd C:\nginx
.\nginx.exe -s reload    # 重载配置
.\nginx.exe -s stop      # 停止
.\nginx.exe              # 启动
```

---

## 十、部署包清单（快速参考）

上传到服务器的最小部署包：

```
D:\photo-management-deploy\
├── photo-management-1.0.0.jar      ← 后端 jar 包
├── application.yml                  ← 后端配置（可按服务器修改）
├── static\                          ← 前端编译产物
│   ├── index.html
│   └── assets\
├── sql\                             ← 数据库初始化脚本
│   ├── init.sql
│   ├── add-dhash-column.sql
│   └── ...
├── uploads\                         ← 上传文件目录
├── images\                          ← 原始图片目录
├── thumbnails\                      ← 缩略图目录
└── logs\                            ← 日志目录
```
